/// <reference types="node" />
import { Transport } from './transport';
import { SocksProxy } from 'socks';
export declare class TlsTransport extends Transport<{
    url: string;
    enableTrace?: boolean;
    proxyOptions?: TlsTransportProxyOptions;
}> {
    private socket;
    send(data: Buffer): void;
    connect(): Promise<any>;
    disconnect(): void;
}
export declare type TlsTransportProxyOptions = SocksProxy;
